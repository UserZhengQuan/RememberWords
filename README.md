# RememberWords

A minimal iOS SwiftUI app for remembering words.

## Features
- Add words you want to remember.
- Mark words as remembered.

## Structure
- `RememberWordsApp.swift`: App entry point.
- `ContentView.swift`: Simple word list UI.

## Notes
This repository provides a lightweight SwiftUI example without Xcode project files.
